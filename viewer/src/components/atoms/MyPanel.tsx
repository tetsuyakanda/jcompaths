import React from 'react';

import { Paper } from '@mui/material';
import styled from '@emotion/styled';

const MyPanel = styled(Paper)({ padding: '10px', margin: '5px', overflow: 'auto' });

export default MyPanel;
